<%= form_for(Comment.new, :remote=>true) do |f| %>
  <div class="field">
    <%= f.text_area :body, :placeholder=> "Reply here..", :class=> "reply-body", :required=>true %>
  </div>
  <%= f.hidden_field :article_id, :value=> comment.article_id %>
  <%= f.hidden_field :parent_id, :value=> comment.id %>
  <div class="actions">
    <%= f.submit("Create Reply") %>
  </div>
<% end %>